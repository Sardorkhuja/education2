{
  "name": "education",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "A personal university planner with optional private Supabase sync.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
